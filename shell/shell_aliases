alias zgrep='zgrep --color=always'

# Archive
alias untar='tar -xvzf'

# Arch Linux only
if command -v pacman > /dev/null; then
  alias pacman='sudo pacman --color=auto'
  # System update
  alias update='aur sync -u && pacman -Syu --noconfirm'
fi
